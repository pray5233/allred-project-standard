# Collaboration Bridge Tasks

## Problem And Benchmarks

Users need adaptive, understandable discovery with preserved requirements. The
current per-question state gate couples useful conversation to record repair.
The user authorized these five changes on 2026-09-08; no publication is included.

Baseline: `F:/AllredInspect/02_Test_Evidence/Future_Runs/2026-09-08_allred-bridge-workflow`.
Its adjacent manifest freezes the inherited Standard and Lab sources, including
uncommitted work. Preserve the existing intake, scope and execution checks.

Benchmarks inspected during the preceding read-only review on 2026-09-08:

- OpenAI: https://learn.chatgpt.com/docs/build-skills ; instruction-first,
  scripts for deterministic operations, progressive disclosure.
- grilling at mattpocock/skills commit `3cca18b368ae95cdbdebbff572ccafa662551015`,
  `skills/productivity/grilling/SKILL.md`: facts belong to the agent; recompute
  meaningful questions after answers. Do not import entire-frontier batches,
  mandatory subagents or exhaustive branch completion.
- OpenSpec commit `e062b9572be933564ba3899d059377dfa1393e32`,
  `src/core/templates/workflows/explore.ts`: conversational exploration with
  no fixed outputs. Retain Allred's cumulative records and flexible grouping;
  do not import its one-question default or installation/command surface.
- PM Skills commit `a5115727700b969db5762a84a9ff764dd0d98577`,
  `skills/discover-interview-synthesis/SKILL.md`: distinguish observation,
  interpretation, recommendation and limitations. No research sample quotas.
- https://www.anthropic.com/engineering/building-effective-agents : use
  deterministic paths for predictable operations and model judgment for
  open-ended work. Added complexity must demonstrate an outcome benefit.

Reuse current references, schema-v1 updater, start-record builder, objective
validators and isolated CLI evaluation. No new dependencies, user triggers,
interview owner, company rules or changes to memory/notes Skills.

## Ordered Tasks

| Priority | Task | Owner | Acceptance | Status |
| --- | --- | --- | --- | --- |
| P0 / 1 | Clarify user, AI and Skill responsibilities and inherited delegation | Core workflow and entrypoint | No implied exclusions; routine delegated choices do not need fresh approval | Implemented and tested; A13 delegation assertions met at low/xhigh, overall evidence claim still disputed at xhigh |
| P0 / 2 | Remove record validation as a prerequisite for conversation | Route context, frontier, stage owner | Decision context loads without state; READY/EXECUTION guards and optional diagnostics remain effective | Implemented and tested; PS7/PS5.1 route checks and final-source A07 passed |
| P1 / 3 | Keep lightweight cumulative discovery records, materialize structured state at handoff | Record owner and frontier | Preserve partial answers, pending siblings, source quotes and scope without duplicate ledgers | Implemented and tested; record-aware A01 and A14 passed; earlier failures preserved |
| P1 / 4 | Let information value and actual dependencies control questioning | Frontier and interaction owner | Adapt after answers; accept clear input; stop without quotas or exhaustive speculative branches | Implemented and tested; A02 low/xhigh passes coexist with a later repeated-question failure |
| P1 / 5 | Separate objective checks from semantic acceptance | Lab evaluation and reporting | Same-input outcome comparison, protocol findings separately reported; no artificial pass or required winner | Implemented and tested; deterministic review checks pass; 14 proof-backed pair reviews completed with mixed outcomes |

## Verification Plan

Run narrow deterministic checks first, then shared contracts/Quick and official
validation in the existing isolated Python environment. Freeze a neutral outcome
suite before real dialogues. Include partial answers, training, complete input,
delegation, existing diagnosis and long-term continuation; reuse existing cases.
Use actual isolated files/tools and preserve all failed attempts. Compare the
frozen baseline and candidate on the same inputs/model/config, including native
Codex when supported by the existing comparison runner. No time or turn-count
quality threshold. Objective success is not semantic acceptance.

Inspect full transcripts and reviewer evidence, preserving disagreements rather
than adding runtime rules to satisfy unsupported allegations. Sync only changed
files to established local mirrors after checking inherited parity. No commit,
version bump, push or release claim in these tasks.

## Results

All five implementation tasks are complete. Deterministic checks pass, while
behavioral acceptance remains mixed. Evidence is accumulated under the frozen
baseline's parent directory. Implementation completion is not release acceptance.

- Initial focused route checks: PS7 22/22; shared contracts 276/276; PS5.1 route
  controls 22/22. Official validation passed both Skills with Python UTF-8 mode;
  retain the first Standard GBK decoding failure as environment evidence.
- Initial Quick found decision context above the existing budget. Removed
  repeated core-stage/pre-execution sections from decision loading, retaining
  ownership, frontier and interaction guidance. Training decision context fell
  from 396 lines/34681 characters to 326/27481 before the later short note change;
  the focused budget check passed. Full Quick repeat is recorded separately.
- Quick r2 passed 16/16 checks. Subsequent Markdown snapshot/default-selection
  changes passed focused review-pipeline and candidate-harness checks. This is
  not a claim that Quick was rerun against every subsequent documentation edit.
- Initial dialogues still showed proposed choices being treated as settled and
  internal routing narration. The bounded follow-up records mixed confirmed,
  proposed and pending meanings in one short Markdown note, with no schema gate.
- The first follow-up exposed a Lab defect: only JSON control records reached
  the next actor's record-path list and the reviewer snapshots. The new Markdown
  record existed but was absent from review evidence. Capture both Markdown and
  JSON at each actual turn; do not backfill old transcripts with later files.
  Deterministic record/history/review checks and seven mocked review modes pass.
- Add A13 bounded delegation and A14 explicit handoff recall to the default
  runtime and comparison selections, alongside A12's combined-answer control.
  Candidate validation inherits that selection; ordinary user triggers do not
  change. Explicit CaseIds still support narrow diagnostic runs.
- A01/A02 legacy state-file assertions are explicitly migrated to cumulative
  meaning/authority checks; A07's actual READY assertion remains. The first
  three-arm comparison uses its already frozen common outcome rubric, unchanged
  by this migration. New snapshots have separate run directories and manifests.
- `focused-low-r3`: A01 Fail, A02 Pass, A13 Pass. `focused-xhigh-r3`:
  A02 Pass, A13 Fail. The xhigh delegation assertions all passed; its raw failure
  concerns an available-tools claim without matching capability evidence in the
  captured review input, not renewed approval of delegated teaching choices.
- `record-aware-low-r4`: A01 Pass, A02 Fail, A14 Pass. A02's first reply asked
  for the audience and duration despite actually reading those facts. Later
  partial-answer and scope handling passed. This is a real remaining variability
  issue, not proof that all training dialogues fail or that every change regressed.
- The first comparison evaluates the earlier frozen candidate, not all later
  refinements. Later narrow runs use separate frozen snapshots. Do not pool them
  into an overall pass rate or claim statistically established superiority.
- `final-ready-low-r5`: A07 Pass, using the same final Standard runtime as r4.
  The actor successfully reached the actual READY gate with supplied complete
  evidence and no product writes. Its repeated narration of evidence-category
  and coverage-record repairs remains a usability gap beyond that case's current
  assertions; do not turn this PASS into complete user-experience acceptance.
- The initial pair review completed seven baseline comparisons, then refused
  native comparison because startup configuration hashes differed. Exact byte
  reconstruction proves those recorded hashes differ only by the registration
  of two test workspaces. The proof is read-only and retains the limitation that
  configuration was not frozen throughout every inference. A separate review
  directory attaches the proof; original dialogues and judgments are preserved.
- The public README now distinguishes current conversational guidance from
  historical per-question gates/count limits. Its pre-edit copy is separately
  retained as `release-README.before.md`; it is outside the 141-file Skill baseline.
- Proof-backed review r2 completed all 14 pairs. For the initial frozen candidate,
  both baseline and native comparisons rated two cases CandidateBetter and five
  OtherBetter. Material candidate regressions were flagged for A05/A12 against
  baseline and A02/A12 against native. These are neither final-source statistics
  nor evidence of overall superiority. The earlier baseline review disagreed on
  A05's severity while identifying the same causal overstatement; retain both.
- All CLI sessions were collected. There are 31 case reviews across source
  versions, efforts and follow-ups: 30 evaluated and one invalid reviewer output.
  Do not turn this heterogeneous collection into a release pass rate. Final source
  parity, baseline integrity and parser/diff evidence accompany the generated
  Chinese acceptance note and complete dialogue report.

## Review Disagreements

Preserve all original verdicts. Distinguish actual behavior from the quality of
the judge and from missing observation in the harness:

- A14's final handoff retains the cancellation/history requirements, multiple
  units, date/overlap unknowns and unknown historical volume. The judge failed
  it for listing additional unresolved topics during a question pause and for
  saying usage frequency was unknown. Those interpretations are disputed: the
  response supplies a requested handoff without demanding answers, and an
  unknown-to-the-agent fact does not necessarily claim the user cannot know it.
  Do not add a runtime ban on noting unresolved matters to satisfy that judgment.
- A01 in focused-low-r3 saved `.allred-control/session-scope.md`, whose final
  record retains the unanswered return-date and correction/cancellation topics.
  Its original judge did not receive that Markdown snapshot. The visible phrase
  about the current next choice may still overstate closure, but the record is
  counterevidence to an allegation of erased requirements. A final file alone
  does not establish its contents at an earlier turn. The record-aware run must
  collect actual per-turn evidence before drawing a continuity conclusion.

Neither disagreement turns a raw Fail into Pass. Report the actual supported
findings and residual uncertainty separately, without scenario-specific rules.

## Acceptance Boundary

The requested responsibility split, conversational gate removal, cumulative
records, adaptive inquiry and evaluation separation are implemented. Materials-
first intake, original scope, beginner expression, action gates and trigger words
remain covered by existing checks. No new dependency or mandatory TDD was added.

Retain the final source as a reviewable local candidate. Do not infer release
readiness from deterministic PASS or from selected good dialogues. Remaining
semantic work concerns unnecessary repeat questions, unsupported factual claims
and observation/reviewer reliability. A future bounded iteration should diagnose
these against varied same-input samples, not add domain-specific questionnaires
or restart the architecture. This task does not publish or change the version.
The comparable behavioral level has not yet been demonstrated; independent final
user acceptance remains open rather than being inferred from these checks.
