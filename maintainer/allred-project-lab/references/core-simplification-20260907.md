# Core Simplification And Controlled Comparison

Status: scoped implementation and evaluation complete; behavioral acceptance not
met. Internal candidate only; no commit or release approval.

## Problem And Baseline

Ordinary project users need deep, adaptive discovery without managing state-file
repairs. Recent A01/A02 evidence shows choices stored as Q facts bypassing the
DECISION guard, repeated representations, and internal diagnostics in progress.
Reviewer corrections on unchanged transcripts are not conversation improvements.

The complete pre-change Standard and Lab are frozen at
`F:/AllredInspect/02_Test_Evidence/Future_Runs/2026-09-07_allred-core-simplification/baseline`.
The adjacent hash manifest covers all 117 files, including inherited local edits.
This is a comparison baseline, not an assertion of accepted release quality.

## Benchmarks And Deliberate Differences

- Local rc15: preserve material-first intake, partial-answer continuity, existing
  schema-v1 state, exact start authorization, scope provenance, conditional routes,
  and the real CLI/evidence-grounded reviewer. No new dependency is needed.
- PM Skills, commit `77cf4246515e81b944b8f3e7912a6afa1f8bc0ce`, inspected 2026-09-07:
  `skills/deliver-prd/SKILL.md` separates optional sections and consumes prior work;
  `docs/internal/release-plans/v2.27.0/records/output-eval-informed-20260615.md`
  compares full instructions, structure-only and ordinary generation. Reuse
  explicit ownership, conditional detail and comparison, not its PM-only method,
  per-step approvals, fixed interview counts or Claude Workflow dependency.
  Source: https://github.com/product-on-purpose/pm-skills/tree/77cf4246515e81b944b8f3e7912a6afa1f8bc0ce
- Official installed skill-creator: keep only decision-changing guidance and
  deterministic mechanics. Matt grilling's dependency frontier remains the
  interview pattern. No external Skill invocation is added.

## Implementation And Acceptance

1. Freeze: retain original sources, reviewer and cases; write the comparison
   criteria before observing new outputs. Do not score elapsed time or turn count.
2. Runtime: keep known facts in U/E sources without duplicate Q copies and use D
   nodes for consequential choices. The cumulative Q/D queue must retain unresolved
   required outcomes and unanswered facets, including those outside the next packet.
   All state-bound packets get the same stage/frontier check regardless of Q/D.
   Initial factual intake can be unbound; this has readability authority only.
   Machine validation never certifies interpretation of a question or quote.
3. Interaction: consolidate the round procedure in its canonical frontier owner;
   keep user updates about discoveries, impact and next work, not internal repair.
   Preserve no-material answers, meaningful follow-ups and complete-input closure.
4. Lab: compare A=frozen full Skill, B=changed Skill, C=native Codex with the same
   cases, tools, model settings and ordinary task boundaries. Freeze a shared
   outcome rubric without Skill-specific call requirements; retain the original
   runtime-contract tests separately. A tie or loss is valid, never a reason to
   change the judge until B wins. Synthetic fixed turns are bounded tests, not
   proof of unrestricted discovery or real-user acceptance.
5. Verify: focused script regressions, PS7/PS5.1, Quick, official validation,
   actual CLI dialogues, separate reviewer evidence and local mirror parity.
   Report changed-case and six-route coverage separately from high-effort and
   full release coverage. No package publication or original project writes.

## Progress

- [x] Read current owners and external benchmark; freeze pre-change sources.
- [x] Simplify the runtime round and remove label-based stage exemption.
- [x] Freeze and run the three-arm comparison using actual tools.
- [x] Review complete dialogues and run compatibility and scoped final checks.
- [x] Synchronize mirrors and verify frozen evidence preservation in final-audit.json.
- [x] Report outcomes and remaining gaps without promoting partial evidence.
- [ ] Meet behavioral acceptance and release criteria; not achieved in this batch.

## Observed Interface Follow-Up

The first frozen candidate's actual A01 turn 2 passed an in-process question
check, then failed an equivalent PowerShell `-File` call because `D1,D2` arrived
as one ID. Normalize comma-separated CLI IDs before the existing eligibility
checks; reject duplicates and empty IDs. This is a transport fix, not semantic
approval. Keep the original three-arm run unchanged; test the updated interface
separately with fresh dialogue evidence. Routine diagnostic narration is still
observed and must not be reported as solved by the first candidate.

## Comparison Integrity

The maintainer comparison records worker exit codes and checks complete case
accounting, runtime/source hash agreement and harness snapshot integrity before
pairwise review. Exit 1 can represent a valid observed behavioral failure; it
cannot turn a contradictory all-pass summary into acceptance. The dependency-free
negative checks are part of Quick. `-ValidateEvidenceOnly` checks existing evidence
without new model calls and explicitly does not award behavioral acceptance.
Older frozen runs lack worker records, so their parent process outcome and logs
must also be reviewed. This addition changes the Lab, not the runtime workflow.

## Observed Configuration Registration Follow-Up

The original comparison correctly stopped when all three starting config hashes
differed. Read-only byte reconstruction then reproduced all three exact hashes
by retaining zero, one and two of the CLI's fixture trust registrations. No config
contents were exported or edited. This is evidence of additive workspace
registration, not proof of a changed model/provider, and does not freeze every
later call. Keep the original hashes and blocked attempt visible.

An explicit `-ConfigEquivalenceEvidenceRoots` option now permits only this proven
case: derive exact fixture workspace paths from existing dialogue evidence,
recognize the CLI's single-key literal trust records, and reconstruct every old
hash from current bytes. Unknown formats, other paths, extra settings or model
changes fail closed. The narrow helper is byte forensics, not a TOML parser or a
rule to ignore all project configuration. The resumed comparison uses unchanged
dialogues and criteria; record it as a protocol follow-up, not the original
uninterrupted run. This mechanism remains Lab-only.

The first visible-only pair reviewer mistook an assistant's source restatement
for the supplied material. Preserve that incomplete review set. The revised
review packet includes the common original `case.files` next to both dialogues,
retains the frozen criteria, and writes into a fresh `-ReviewOutputRoot` with a
protocol/hash record. A comparison winner still cannot substitute for actual
tool/semantic acceptance. `material_regression` is the reviewer's direction-neutral
raw flag; `candidate_regression` requires an actual candidate loss as well.

## Cumulative Queue Follow-Up

The grounded comparison preferred the old version in A02/A03. Inspecting the
candidate's actual last state found no pending node for earlier incomplete
case facts or the required manager outcome, although coverage remained open.
This does not prove those topics could never be revisited, but it exposes an
ambiguity introduced by calling Q rows optional. Keep known facts only in U/E;
require the cumulative queue to retain unanswered facets already asked and
unresolved required outcomes, including nodes outside the next visible packet.
No new topic, domain rule or interview-count limit is added.

Also remove the stale Q-only exception from the interaction owner and clarify
the state-gate reference. The script already applied the bound-packet rule;
contradictory prose must not direct models to the older path. This B2 runtime
follow-up uses fresh A02/A03 dialogues in `frontier-followup-low` with the same
frozen common assertions. B0 comparative results cannot count as B2 acceptance.

## Completed Evaluation

The main experiment completed 21 dialogues / 45 user turns on gpt-5.6-sol low;
B1 added 3 / 6 and B2 added 2 / 5, for 26 / 56 total. These are real CLI calls
with fixed synthetic user turns and tool-aware history replay, not unrestricted
interviews. The 14 material-grounded pair reviews compare B0 only: 4 wins, 1 tie,
2 losses versus baseline; 4 wins, 3 losses versus native. Reviewer mistakes and
mixed absolute results prevent acceptance from being inferred from these counts.

B2 A02/A03 both failed. The reviewer found internal diagnostic narration and, in
A03, insufficient support for a broad local-benchmark claim. Separately, maintainer
inspection found missing unanswered facets in both states despite open coverage.
Correct queue instructions did not establish correct runtime behavior. Do not
report those omissions as fixed or as proven final scope deletion.

Final runtime contracts are 100/100; PS7 and PS5.1 interface checks are 72/72 each.
Quick passed 13/13 before the later configuration helper and B2. Those later edits
received scoped checks: comparison contracts 19/19, B2 structure/invariant/
generality/route checks and official validation of both Skills. These checks do
not certify user-answer interpretation. No high-effort matrix, repeated stability
acceptance or cross-machine installation test was performed.

Next work should first calibrate reviewer semantics on retained evidence, then
isolate answer-to-state mapping: accept explicit combined answers, retain sibling
unknowns, and never promote uncertainty into a confirmed choice. Preserve mature
intake and authorization. Do not broaden the redesign or add scenario-specific
rules based on this batch. Full evidence and maintainer disagreements are in
`F:/AllredInspect/02_Test_Evidence/Future_Runs/2026-09-07_allred-core-simplification/report.md`.
