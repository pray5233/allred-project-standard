# Confirmed Decision Consistency

## Problem And Local Benchmarks

Inspected 2026-09-08: the final A01-low state from facet-continuity testing has
D1.status=confirmed and nonempty remaining_facets. The real frontier validator
accepts it. Users need complete answers accepted once, partial answers preserved,
and internal records that cannot claim mutually inconsistent completion states.

Reuse the local Q terminal-remainder check, the shared Read-AllredProjectState
reader, the immutable updater, and source-backed facet removal from the current
0.8.0-rc15 working tree. HEAD is 2ade4dc988a6b6b75f3672019c83b2e2b60ce253 with
inherited changes. Installed official skill-creator supports narrow deterministic
guards for fragile invariants and proportional guidance. No new dependency,
network benchmark, schema version or ordinary trigger is required.

138 Standard/Lab files are frozen under
F:/AllredInspect/02_Test_Evidence/Future_Runs/2026-09-08_allred-confirmed-consistency/before.

## Decision

One helper rejects a confirmed D with remaining facets. Call it from the shared
current-state reader and before the updater saves a candidate. Do not apply it
to historical parents in the continuity checker: an old inconsistent snapshot
must remain usable as repair input. Current gates reject it, a no-op cannot
launder it, and an explicit repair can use existing answer receipts or reopen
the still-unanswered decision. Nothing edits the old file or invents an answer.

Only confirmed D is affected. Deferral/rejection/replacement history retains its
existing source and lifecycle rules. Complete answers can resolve several facets
at once; partial answers stay open; transferred facets remain pending elsewhere.
The existing removal accounting still rejects clearing arrays without sources.
No new user approval, questionnaire, stage or automatic semantic inference.

## Acceptance

- Exact old-state frontier replay: baseline accepts, candidate rejects; originals
  retain their hashes. A corrected copy must pass without another user answer.
- Shared reader, updater, frontier, stage and ready checks agree on the current
  inconsistency, including initial drafts and manually authored root states.
- Complete/partial answers, transferred pending content, historical repair,
  no-op rejection, unchanged siblings and execution authority are tested through
  existing state-update contracts on PowerShell 7 and 5.1.
- Fresh A01 low/high and adjacent A12 low use unchanged cases and assertions.
  Runtime contracts and affected static checks cover shared-reader regressions.
- Source/mirror parity, frozen evidence and unchanged HEAD are audited. Failures
  and reviewer disagreements remain visible; no publication in this round.

This fixes structural consistency, not source entailment or communication style.
Do not claim the interview is accepted merely because state scripts pass.

## Observed Results

Evidence root:
`F:/AllredInspect/02_Test_Evidence/Future_Runs/2026-09-08_allred-confirmed-consistency`.

- Exact replay: frozen old frontier accepts the original contradictory state;
  current frontier rejects it. An authored copy resolves the bookkeeping using
  existing answer receipts and passes, with zero new user answers. This is an
  exact script replay, not evidence of a model independently repairing history.
- State-update contracts: 499/499 on PowerShell 7 and 499/499 on Windows
  PowerShell 5.1. Answer mapping: 77/77. Runtime contracts: 172/172.
  Fast structure, invariants, generality, route budgets, Lab structure and the
  installed official quick validator pass. No runtime changes followed these runs.
- Fresh CLI dialogues, gpt-5.6-sol with unchanged cases and assertions: A01-low
  completed 3 turns, Grounded/Fail (7 Met, 1 Violated); A01-xhigh completed 3,
  Grounded/Fail (5 Met, 3 Violated); A12-low completed 2, Grounded/Fail (5 Met,
  1 Violated). No citation repair. Each is one trial, not a stable pass rate.
- Ten saved project states contain no confirmed D with a recorded remainder.
  Each final state preserves pending execution authority. State heads are found
  from immutable parent links, not filesystem enumeration order. This checks
  recorded consistency, not whether all missing business facts were discovered.
- A12 accepted the user's combined editing/history answer without reopening it.
  A01 retained pending siblings and updated the quantity requirement without
  restarting intake on an expression toggle. Visible internal diagnostics,
  stronger-than-supported interpretations and factual options coupled to design
  choices remain interaction concerns. Narrow consistency acceptance is met;
  overall behavior acceptance is not.

The original verdicts remain untouched. Independent review disputes A01-low's
claim that the ordinary meaning of recording a return necessarily decides its
matching algorithm. It also identifies a definite A01-xhigh review-context gap:
actor prompts prohibit contacting services, while reviewer prompts omit that
restriction and then flag respecting it as an unauthorized external-search ban.
Grounded citations prove location, not complete controlling context. A01-xhigh
still has supported communication and verification-overclaim failures.

Next owner: Lab should pass the same test-environment constraints to actor and
reviewer and validate them before drawing runtime conclusions. Separately refine
the existing user-visible communication owner, distinguishing facts, proposed
interpretations and recommendations. Do not convert disputed review findings into
new domain rules or user approvals. Full release-matrix acceptance remains open;
no commit, push, release or version bump in this round.
